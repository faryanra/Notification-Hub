{
  "name": "notification-hub-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build:free": "node tools/build-free.mjs",
    "build:pro": "node tools/build-pro.mjs",
    "build": "npm run build:free && npm run build:pro"
  }
}
