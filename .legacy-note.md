# Legacy Code Note

The following folders contain legacy code from v1.x:

- `core/`
- `modules/`
- `integrations/`
- `api/`

These folders are **still active** and will run alongside the new architecture in `src/`.

User wants to test the plugin **without** legacy code to ensure the new architecture works independently.

## To Remove Legacy Code:

1. Delete `core/` folder
2. Delete `modules/` folder
3. Delete `integrations/` folder (legacy)
4. Delete `api/` folder
5. Update `notification-hub.php` to remove legacy bootstrapping code

## User Request:

> "نمیخوام هیچ فایل قدیمی باشه که ازش هیچ استفاده ای بشه اگه کرش میخواد کنه بزار بکنه !!!!"
>
> Translation: "I don't want any old files to be used at all. If it's going to crash, let it crash!!!!"
